require "spec_helper"

RSpec.describe Cli::Data::Gem::Cbituin do
  it "has a version number" do
    expect(Cli::Data::Gem::Cbituin::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
